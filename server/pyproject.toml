[project]
name = "minichat-server"
version = "0.1.0"
description = "Simple WebSocket chat server with web and CLI clients"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
	"fastapi>=0.95.2",
	"uvicorn[standard]>=0.22.0",
	"websockets>=11.0.3"
]
